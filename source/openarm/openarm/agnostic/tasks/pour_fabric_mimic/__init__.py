"""pour_fabric_mimic — 양팔 물붓기, **저차원(언더액추·PhysX mimic) 손 전용** 트랙.

`pour_fabric`(DG-5F 20관절, 09.13 text2reward 트랙)의 사본이다. 원본은 손대지 않는다
(사용자 결정 09.14: "원본 냅두고 저차원 핸드 전용을 새로 만든다"). 보상은 여기도 없다 —
`reward_gen/pour_bi_rh/iter_NN/compute_reward.py` 를 `env.reward_code_path` 로 읽는다.
"""
